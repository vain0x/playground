{
    int i = 1;

    while (i <= 15) {
        if (i % 15 == 0) {
            sprint ("FizzBuzz");
        } else if (i % 3 == 0) {
            sprint ("Fizz");
        } else if (i % 5 == 0) {
            sprint ("Buzz");
        } else {
            iprint (i);
        }
        i = i + 1;
    }
}
