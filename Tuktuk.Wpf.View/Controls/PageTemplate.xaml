<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Tuktuk.Wpf.Controls;assembly=Tuktuk.Wpf.Model"
    >
    <DataTemplate DataType="{x:Type controls:Page}">
        <DockPanel>
            <DockPanel DockPanel.Dock="Top">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <Button Content="←" />
                    <Button Content="→" />
                </StackPanel>

                <ContentPresenter Content="{Binding AncestorList}" />
            </DockPanel>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ContentPresenter Content="{Binding FileTree}" />

                <GridSplitter Grid.Column="1" />

                <ContentPresenter
                    Grid.Column="2"
                    Content="{Binding FileCollection}"
                    />
            </Grid>
        </DockPanel>
    </DataTemplate>
</ResourceDictionary>
